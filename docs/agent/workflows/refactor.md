# Workflow：重构

## 目标

重构必须保持外部行为不变，并让边界、测试或维护性明确变好。

## 步骤

1. 阅读通用文档和相关模块文档。
2. 明确重构目标、非目标和外部行为不变的证据。
3. 查找现有测试；不足时先补行为保护测试。
4. 分小步修改，避免混入功能变化。
5. 运行相关验证。
6. 如果边界变化，更新架构和模块文档。

## 禁止事项

- 不允许在重构中夹带未声明的新功能。
- 不允许改变公共接口而不更新调用方和文档。
- 不允许为了抽象而抽象。
