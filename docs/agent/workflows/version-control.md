# Workflow：版本控制

## 目标

版本控制操作必须保护用户改动，提交范围清晰，推送必须获得明确授权。

## 步骤

1. 执行版本控制操作前检查工作区状态。
2. 区分本次改动和用户已有改动。
3. 只提交本次相关文件。
4. 提交前运行相关验证，无法验证时在报告中说明。
5. 推送、发布、打标签或变基必须获得用户明确授权。

## 禁止事项

- 不允许使用 `git reset --hard` 或覆盖用户改动，除非用户明确要求。
- 不允许把密钥、私密对话、语音样本或本地路径提交进仓库。
- 不允许因为任务完成就主动推送到 remote。
